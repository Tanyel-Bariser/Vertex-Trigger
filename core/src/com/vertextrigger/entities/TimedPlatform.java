package com.vertextrigger.entities;

/**
 * Platform that does something, such as disappears/reappears,
 * counts down then explodes or spikes go in & out of platform, etc.
 */
public abstract class TimedPlatform implements Entity {

	
}
